module Lib
  ( main
  ) where

{-
  WIP: project idea: a tool that does all boring stack routines:

  - remove junks from stack.yaml
  - sort and dedup dependencies in package.yaml
  - check what is the lastest lts compatible with certain version of GHC
  - find project home by going upwards and run commands with PROJECT_HOME set.

 -}

main :: IO ()
main = pure ()
